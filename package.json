{
  "name": "backroom-sim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "devDependencies": {
    "vitest": "^4.0.0",
    "typescript": "~5.8.0"
  },
  "scripts": {
    "dev": "npm run dev -w backroom-viewer",
    "build": "npm run build -w backroom-viewer",
    "test": "vitest run",
    "typecheck": "tsc --build --noEmit"
  }
}
